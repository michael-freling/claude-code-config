# Cert-Manager

- Creates Certificate, CertificateRequest, Order, Challenge resources
- Check `.status.conditions` for detailed failure reasons
- ACME challenges may fail due to DNS/HTTP verification issues
